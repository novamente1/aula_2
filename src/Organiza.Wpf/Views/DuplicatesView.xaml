<UserControl x:Class="Organiza.Wpf.Views.DuplicatesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
    <DockPanel IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}">
        <StackPanel DockPanel.Dock="Top">
            <TextBlock Text="Verificar documentos e cópias" Style="{StaticResource Heading}" />
            <TextBlock Text="Um clique examina todos os documentos. Cópia exata exige tamanho + SHA-256 completo; o nome nunca entra no cálculo." Opacity="0.72" TextWrapping="Wrap" />
            <StackPanel Orientation="Horizontal" Margin="0,18,0,14">
                <Button Content="1. Verificar todos agora" Command="{Binding ScanCommand}"
                        Background="{StaticResource AccentBrush}" Foreground="White" BorderThickness="0" />
                <Button Content="2. Mover cópias selecionadas para a pasta de duplicados" Command="{Binding MoveCommand}" Margin="10,0,0,0" />
                <Button Content="3. Manter cópias no lugar e continuar" Command="{Binding ConfirmReviewCommand}" Margin="10,0,0,0" />
            </StackPanel>
            <TextBlock Text="{Binding Status}" Margin="0,0,0,14" />
        </StackPanel>
        <ListBox ItemsSource="{Binding Groups}" ScrollViewer.VerticalScrollBarVisibility="Auto"
                 BorderThickness="0" Background="Transparent"
                 VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"
                 ScrollViewer.CanContentScroll="True">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" CornerRadius="8" Padding="16" Margin="0,0,0,12">
                            <StackPanel>
                                <TextBlock FontWeight="SemiBold">
                                    <Run Text="Grupo — " /><Run Text="{Binding SizeBytes, Mode=OneWay}" /><Run Text=" bytes" />
                                </TextBlock>
                                <TextBlock Text="{Binding Sha256}" FontFamily="Consolas" FontSize="11" Opacity="0.6" Margin="0,4,0,8" />
                                <ItemsControl ItemsSource="{Binding Files}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,3">
                                                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="120" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                                                <CheckBox IsChecked="{Binding IsSelected}" IsEnabled="{Binding CanMove}" Margin="0,0,10,0" />
                                                <TextBlock Grid.Column="1" Text="{Binding ActionLabel}" Foreground="#227447" FontWeight="SemiBold" Margin="0,0,12,0" />
                                                <TextBlock Grid.Column="2" Text="{Binding RelativePath}" TextTrimming="CharacterEllipsis" ToolTip="{Binding FullPath}" />
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
        </ListBox>
    </DockPanel>
        <Border Panel.ZIndex="20" Background="#D9F5F7FA"
                Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Border Background="White" BorderBrush="#B9CCF5" BorderThickness="1" CornerRadius="10"
                    Padding="28" Width="500" HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="Processando... Aguarde a conclusão da etapa" FontSize="18" FontWeight="SemiBold"
                               TextAlignment="Center" Margin="0,0,0,12" />
                    <TextBlock Text="{Binding OperationStatus}" TextWrapping="Wrap" TextAlignment="Center" Margin="0,0,0,16" />
                    <ProgressBar IsIndeterminate="True" Height="10" />
                </StackPanel>
            </Border>
        </Border>
    </Grid>
</UserControl>
