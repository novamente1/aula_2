<Window x:Class="Organiza.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding AppTitle}" Height="760" Width="1180" MinHeight="650" MinWidth="980"
        WindowStartupLocation="CenterScreen" Background="{StaticResource BackgroundBrush}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="215" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0" Background="{StaticResource SidebarBrush}">
            <DockPanel Margin="14,22">
                <StackPanel DockPanel.Dock="Top">
                    <TextBlock Text="{Binding AppTitle}" Foreground="White" FontSize="20" FontWeight="Bold" Margin="8,0,0,16" />
                    <TextBlock Text="{Binding WorkflowStatus}" Foreground="#C7D3E8" FontSize="11" TextWrapping="Wrap" Margin="8,0,4,16" />
                    <Button Content="1. Selecionar pasta" Command="{Binding SelectFolderCommand}" Margin="0,0,0,8" HorizontalContentAlignment="Left" />
                    <Button Content="2. Verificar documentos" Command="{Binding DuplicatesCommand}" Margin="0,0,0,8" HorizontalContentAlignment="Left" />
                    <Button Content="3. Organizar e renomear" Command="{Binding OrganizeCommand}" Margin="0,0,0,8" HorizontalContentAlignment="Left" />
                    <Button Content="4. Dividir PDFs grandes" Command="{Binding PdfSplitCommand}" Margin="0,0,0,8" HorizontalContentAlignment="Left" />
                    <Button Content="5. Histórico" Command="{Binding HistoryCommand}" Margin="0,0,0,8" HorizontalContentAlignment="Left" />
                    <Button Content="6. Configurações" Command="{Binding SettingsCommand}" HorizontalContentAlignment="Left" />
                </StackPanel>
                <TextBlock DockPanel.Dock="Bottom" Text="Arquivos originais protegidos" Foreground="#AFC0DF" Margin="10" />
            </DockPanel>
        </Border>
        <ContentControl Grid.Column="1" Content="{Binding CurrentPage}" Margin="24,24" />
        <Border Grid.ColumnSpan="2" Panel.ZIndex="100" Background="#D9F5F7FA"
                Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Border Width="560" Padding="30" HorizontalAlignment="Center" VerticalAlignment="Center"
                    Background="White" BorderBrush="#78B98F" BorderThickness="1" CornerRadius="10">
                <StackPanel>
                    <TextBlock Text="Processando... Aguarde a conclusão da etapa" FontSize="19" FontWeight="SemiBold" TextAlignment="Center"/>
                    <TextBlock Text="{Binding CurrentOperation}" Margin="0,12,0,16" TextWrapping="Wrap" TextAlignment="Center"/>
                    <ProgressBar IsIndeterminate="True" Height="10"/>
                    <TextBlock Text="A navegação e os cliques ficam bloqueados até a conclusão segura." Margin="0,12,0,0" Opacity="0.7" TextAlignment="Center"/>
                </StackPanel>
            </Border>
        </Border>
    </Grid>
</Window>
